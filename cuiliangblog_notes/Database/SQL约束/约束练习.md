# 约束练习
    1. 为班级表的班号追加主键约束
+ alter  table class add primary key(cno);
    1. 为学生表的学号追加主键约束
+ alter  table stu add primary key(sno);
    1. 为学生表的班号追加外键约束
+ alter  table stu add constraint fk_stu_cno foreign key(cno) references class(cno);
    1. 查看stu表的外键约束名称
+ select  t.constraint_name,t.table_name,t.column_name,t.referenced_table_name,t.referenced_column_name  from information_schema.key_column_usage t where t.table_schema='test' and  t.table_name='stu' and t.referenced_column_name is not null;
    1. 为BMI的bname和sex列追加主键约束
+ alter  table bmi add primary key(bname,sex);
    1. 为班级表的班名追加非空约束
+ desc  class;
+ alter  table class modify cname varchar(20) not null;
+ desc  class;
    1. 为学生表的姓名追加非空约束
+ desc  stu;
+ alter  table stu modify sname varchar(20) not null;
+ desc  stu;

